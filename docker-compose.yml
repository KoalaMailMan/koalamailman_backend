services:
  nginx:
    container_name: nginx
    build: ./nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /etc/letsencrypt:/etc/letsencrypt:ro
    depends_on:
      - backend
    restart: always
    networks:
      - mandalart

  backend:
    image: ${BACKEND_IMAGE}
    container_name: backend
    expose:
      - "8080"
    env_file:
      - .env
    restart: always
    networks:
      - mandalart

networks:
  mandalart:
    driver: bridge